#include "execution.h"

void	echo(int fd, char *str)
{
	
}


int	main(int argc, char **argv, char **envp)
{
	// int fd;
	(void)argc;
	(void)argv;
	(void)envp;
	// (void)fd;
	// fd = open("env_output.txt", O_RDWR | O_CREAT, 0644);
	// if (fd < 0)
	// 	return (0);
	// pwd(1);

	cd("libft");
	
	
	
	return (0);
}